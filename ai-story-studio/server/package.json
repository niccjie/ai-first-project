{
  "name": "ai-creator-studio-server",
  "version": "0.3.0",
  "private": true,
  "description": "Local Express server for niccjie AI Creator Studio",
  "engines": { "node": ">=22" },
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js",
    "test": "node --test server.test.js"
  },
  "dependencies": { "express": "^5.1.0" }
}
